apiVersion: v1
kind: Service
metadata:
  name: teamboard
  namespace: teamboard
  labels:
    app: teamboard
spec:
  type: NodePort
  selector:
    app: teamboard
  ports:
    - port: 3001
      targetPort: 3001
      nodePort: 30082
      protocol: TCP
